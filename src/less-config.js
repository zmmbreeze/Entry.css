(function($, less) {
    var LessConfig = function() {};
    var LessRender = function() {};
    var LessConfiger = function(config) {
        this.config = config;
    };
    LessConfiger.prototype.rend = function() {};
})(jQuery, less);
